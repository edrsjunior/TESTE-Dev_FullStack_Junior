SISTEMA DE CATALAGO DE CARROS USING:
-Python/FastAPI
-MYSQL
-REACT

0.1: CRIAR O BD COM A MODELAGEM DAS ENTIDADES;
1. DEFINIR O TIPO dos carros no py;
2. CRIAR API COM AS ROTAS NO PY USING FASTAPI;
3. LOGIN WITH JWT VALIDATION(PUBLIC);
CRUD{
    -CADASTRAR ADMINS(ONLY FOR TEST);
    -GET CARS ORDENADO by VALUE(PUBLIC);
    -POST CARS (private, check login token);
=
}